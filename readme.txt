create a virtualenv and install all libs in requirments.txt
Start local server and upload a txt file with a input in the same format as in the dummy_input_file.txt
A graph will be generated corresponding to the inputed values.